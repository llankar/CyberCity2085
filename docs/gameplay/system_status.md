# Gameplay System Status Matrix

| System | Current behavior | Missing behavior | UI entry point | Test coverage |
|---|---|---|---|---|
| funds | Corporate ledger supports spend/add/mission payouts and weekly income sync. | More explicit spend-preview UI before expensive actions. | Corp rooms + squad recruit flow. | `tests/test_funds_management.py`, `tests/test_weekly_corporate_funding.py` |
| calendar | Day/week/month progression ticks daily systems and date labels. | Optional fast-forward planning tools. | Corp/City `D Advance day`. | `tests/test_calendar_management.py`, e2e flow test |
| research | Projects can be started, tick down, complete with unlock flags/modifiers; research tree now includes 21 projects across 7 branches for clearer progression pacing. | Compact dependency-tree presentation for deeper branches and project filtering polish. | Corp research room actions. | `tests/test_research_management.py`, e2e save/load flow |
| events | Strategic events can spawn, be responded to, expire, and render through the command deck safely. | Multi-event response queue UX depth. | Corp/City records + event actions. | `tests/test_strategic_events.py` |
| equipment | Agents can cycle modular loadout slots in armory. | Costed equipment economy and rarity progression. | Squad armory room actions. | `tests/test_agent_equipment.py`, e2e save/load flow |
| stress | Daily stress recovery and mission pressure integration exist; recovery-room support beats can add a small mentor/relationship stress relief line. | Additional proactive stress-management actions. | Squad medbay/briefing summaries. | `tests/test_stress_management.py`, `tests/test_recovery_status.py`, `tests/test_calendar_management.py` |
| recovery | Recovery turns, medbay readiness, recovery-room support dialogues, and latest recovery beats are tracked. | Deeper treatment choices with tradeoffs. | Squad medbay + deployability checks + narrative feed. | `tests/test_recovery_status.py`, `tests/test_recovery_dialogues.py`, `tests/test_calendar_management.py` |
| temporary scars | Serious wounds can create temporary narrative scars visible in dossier lines, mission briefing facts, debrief consequence summaries, and recovery/command-deck panel text. | More scar-specific UI affordances and treatment choices. | Agent dossier, mission briefing, debrief, squad/medbay command deck. | `tests/test_temporary_scars.py`, `tests/test_mission_briefing_view.py`, `tests/test_battle_debrief_view.py` |
| agent reputation | Standout post-mission events can award one-time reputation tags and a first nickname; awards persist on agents and surface in debrief data, event logs, and dossier lines. | More authored thresholds for elite enemies, objective-specific rescues, and long-term reputation consequences. | Mission debrief, narrative feed/event log, agent dossier. | `tests/test_agent_reputation.py`, `tests/test_agent_sheet_schema.py`, `tests/test_battle_debrief_view.py` |
| personality-driven logs | Personality traits now modulate mission complication logs, debrief lines, secondary trait suffixes, and high-stress debrief reactions with neutral fallback for old saves. | More banter variants by relationship and faction context. | Mission event log and post-battle debrief. | `tests/test_personality_traits.py`, `tests/test_narrative_debrief.py` |
| faction rewards | Narrative faction reward journal on mission success. | Systemic faction economy effects (intentionally out-of-scope for now). | Post-mission logs / narrative feed. | `tests/test_faction_rewards.py` |
| core faction content | Faction content now has a core campaign factory covering Starvers, Three Sevens, Pharmacorp, Novatek, raiders, mutants, corporate security, local city factions, and hidden AI factions while preserving the vertical-slice visible faction list. | Hook the broader faction library into more mission/event generation paths when reveals are ready. | Faction data and future campaign hooks. | `tests/test_factions.py` |
| aftermath | Agent aftermath and debrief persistence/logging after missions. | Additional branching aftermath scenes. | Post-battle to command logs. | `tests/test_agent_aftermath.py`, `tests/test_narrative_debrief.py` |
| mission generation | Daily mission board generation with pressure-aware template selection. | District-specific handcrafted mission pools. | Squad ops room / mission board. | `tests/test_mission_generation.py`, `tests/test_mission_board_ui.py` |
| battle objectives | Extraction, sabotage, data-theft, containment, civilian-rescue, and recon-scan objectives have tactical markers, prompts, progress text, success/failure metadata, readable mission-board labels, and legacy objective-branch aliases where relevant. | Larger authored objective pools per faction and map-specific marker placement. | Battle objective marker, interaction key, mission board, mission briefing text. | `tests/test_mission_objectives.py`, `tests/test_objective_branches.py`, `tests/test_battle_hud_ui.py`, `tests/test_mission_board_ui.py` |
| mission map routing | Battle setup and mission briefing thumbnails now use the inferred city or wasteland map pool from `assets/maps`, keeping outside-city missions on wasteland art and inside-city missions on city art. | More authored per-mission map pinning. | Mission briefing and tactical launch. | `tests/test_battle_maps.py`, `tests/test_rpg_view.py` |
| mission token sizing | Non-robot combat tokens render at half scale while robot units keep the full-size token silhouette, so missions read lighter without shrinking machine assets. | More nuanced per-unit silhouette classes and deploy-specific overrides. | Battle setup sprite creation. | `tests/test_battle_maps.py`, `tests/test_rpg_view.py` |
| mission selection world map | The squad mission panel now opens a near-full-screen world map selector using `assets/worldmap/Player_World_Map_1778155726161.png`, with mission pins on the map itself and a compact floating briefing card with separate header rows, English summary lines, and legacy French briefing text normalized at load time instead of a fixed right-side column. | Authored per-mission pin art and richer route-specific labels. | Squad ops room mission selector. | `tests/test_world_map_ui.py`, `tests/test_management_hub_ui.py` |
| global scenario panel | Intel room Global Scenario panel shows current act/title/progress, Hungry Tide percentage, Hungry Tide threat level/affected regions/New York impact/ignored consequences, Three Sevens antagonist pressure, Pharmacorp cure-thread pressure, Novatek hybrid-experiment pressure, gated hidden-AI conspiracy pressure, New York and Warsaw status, discovered intel counts, major known threats, unresolved global events, an intel fragment browser with known/locked entries grouped by act/source/faction, a world-state change feed for major campaign escalations, and act-transition overlay stakes/consequences. | Deeper interaction for browsing long intel archives. | Management Intel room. | `tests/test_campaign_panel.py` |
| tactical action preview | Action deck now shows min/max damage + estimated hit/crit and warns when line-of-fire may collide with allies. | More advanced trajectory rules for non-linear shots. | Battle action panel while selecting a target. | `tests/test_combat_preview.py` |
| combat engine boundary | `game/combat/` now separates pure tactical state/transitions from `BattleView`, with `CombatState` and `CombatEngine` covering turn starts, AP actions, and battle-end checks. | Continue moving target-selection/objective-specific rules out of the view in small slices. | Battle tactical view adapter. | `tests/test_combat_engine.py` |
| Godot combat mission UI | Mission briefing now defaults to a Godot handoff: `game/combat/godot_bridge.py` writes a selected-mission JSON payload, resolves the handoff/project paths before launch so Godot reads the written file, uses the in-app Settings screen browser-selected executable first, launches `godot/combat_missions_ui` when Godot is available, and automatically falls back to local Arcade battle when no executable is configured. The Settings browser now persists the Godot path immediately, and the Godot scene now renders a tactical HUD that stays close to the older mission combat layout with a battlefield grid, deployed agent list, selected support assets appended as player-side tactical units, and right-side action deck. Movement in this Godot slice stays free-form, and the move preview now renders as compact inset diamonds instead of full-tile overlays. | Feed completed Godot combat results back into campaign fallout instead of using the fallback for resolution. | Mission briefing `DEPLOY`; `GameState.combat_mission_ui_engine`. | `tests/test_godot_combat_bridge.py`, `tests/test_save_system.py` |
| tactical move highlight compactness | Movement range overlays render at half-size in the Arcade battle HUD and the Godot battlefield. Godot now anchors those compact diamonds to the active unit and draws them on a tighter preview lattice, keeping the same diamond size while removing the large empty gutters seen between reachable cells. | Optional per-action highlight styles and opacity presets. | Tactical battle view highlight overlays. | `tests/test_battle_hud_ui.py`, `tests/test_godot_move_highlights.py` |

| initiative timeline | Tactical HUD now previews the next 6-8 activations from current turn order + AGI initiative and highlights the active unit; high-impact enemies are tagged THREAT. | Optional portraits/icons and richer threat weighting per weapon kit. | Battle HUD top-right timeline panel. | `tests/test_initiative_timeline.py` |
| combat input hints + end-turn confirm | Battle HUD now shows a compact contextual shortcuts banner and switches hint copy by last input mode (keyboard/mouse vs controller-like buttons). End turn now requires a soft second confirm before consuming remaining AP. | Dedicated gamepad mapping and on-screen button glyphs. | Tactical HUD (status panel zone) while selecting actions/targets. | `tests/test_battle_hud_ui.py` |
| action aftermath causality line | Tactical HUD now renders a short-lived fixed-slot line after move/skill/attack with explicit causal payload (`DMG`, optional `STATUT`, optional `SUPPRESSION created`) to reduce clutter while improving decision readability. | Add richer status taxonomy when future effects (bleed/stun/panic) are introduced. | Tactical HUD fixed feedback slot above action bar. | `tests/ui/test_action_aftermath.py` |
| floating combat text | BattleView now emits short-lived floating text for damage, healing, misses, critical hits, suppression, and status changes through the shared `damage_popups` payload list. | Optional animation tuning and icons per popup category. | Tactical battle view above affected units. | `tests/test_rpg_view.py` |
| combat log side panel | BattleView exposes the latest combat events through a Tab-toggled side panel, with modular combat log builders still covering filtered HUD/expanded views. | Category icon polish inside the live side panel. | Tactical battle Tab toggle. | `tests/ui/test_combat_log_panel.py`, `tests/test_battle_hud_ui.py`, `tests/test_battle_view_controls.py` |
| combat status effects | Units carry `status_effects` for suppressed, bleeding, stunned, burning, contaminated, and panicked; turn start applies HP/AP effects and the HUD renders compact status badges. | More authored sources, cleanses, and faction/objective-specific status interactions. | Tactical battle HUD and turn engine. | `tests/test_combat_engine.py` |
| in-battle pause | Escape opens a battle pause overlay with Resume, Settings, and Abandon actions; Escape closes the overlay without advancing combat. | Confirmation copy for abandon and richer settings return context. | Tactical battle Escape overlay. | `tests/test_battle_hud_ui.py`, `tests/test_battle_view_controls.py` |
